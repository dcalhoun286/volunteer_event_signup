{
  "buildCommand": "yarn build",
  "outputDirectory": "dist",
  "framework": "react",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://api.yourapp.com/:path*"
    }
  ]
}
